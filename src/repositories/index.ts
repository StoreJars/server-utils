export { default as BaseRepository } from './BaseRepository';

/**
 * to reduce number of imports
 * export interfaces here as well, since they would be used int he same context
 */
export { default as IMeta } from '../interfaces/IMeta';
export { default as IRead } from '../interfaces/IRead';
export { default as IWrite } from '../interfaces/IWrite';
export { default as IMetaExtended } from '../interfaces/IMetaExtended';
